import {atom} from "recoil";

export const bodyBackgroundColor = atom({
    key: 'bodyBackgroundColor', // unique ID (with respect to other atoms/selectors)
    default: '', // default value (aka initial value)
});

export const highlightColor = atom({
    key: 'highlightColor', // unique ID (with respect to other atoms/selectors)
    default: '', // default value (aka initial value)
});

export const chosenContentBackgroundColor = atom({
    key: 'chosenContentBackgroundColor', // unique ID (with respect to other atoms/selectors)
    default: '', // default value (aka initial value)
});

export const chosenContentBorderColor = atom({
    key: 'chosenContentBorderColor', // unique ID (with respect to other atoms/selectors)
    default: '', // default value (aka initial value)
});

export const chosenButtonTextColor = atom({
    key: 'chosenButtonTextColor', // unique ID (with respect to other atoms/selectors)
    default: '', // default value (aka initial value)
});

export const chosenTextColor = atom({
    key: 'chosenTextColor', // unique ID (with respect to other atoms/selectors)
    default: '', // default value (aka initial value)
});

export const chosenTitleTextColor = atom({
    key: 'chosenTitleTextColor', // unique ID (with respect to other atoms/selectors)
    default: '', // default value (aka initial value)
});

export const chosenMutedTextColor = atom({
    key: 'chosenMutedTextColor', // unique ID (with respect to other atoms/selectors)
    default: '', // default value (aka initial value)
});

export const chosenFooterTextColor = atom({
    key: 'chosenFooterTextColor', // unique ID (with respect to other atoms/selectors)
    default: '', // default value (aka initial value)
});

